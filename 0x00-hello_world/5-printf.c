#include<stdio.h>

/**
 * main - program entry point
 * function prints argument to the console
 * Return: returns 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
